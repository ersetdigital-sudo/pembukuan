TOKENROUTER_API_KEY=sk-WnrnNeOI1Jx8LMwtF7ZVsJwXnNpiwjm4Ta8BVab16pvBL36I
MODEL=MiniMax-M3

# Supabase (used by the app itself — browser + server)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# API key for external agents (e.g. Hermes) calling POST /api/sales.
# This is NOT a Supabase key — generate your own random string and only
# share this value with the agent, never the Supabase keys above.
AGENT_API_KEY=generate-a-long-random-string-here
